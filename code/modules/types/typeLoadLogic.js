'use strict';

// import { getTasks } from "../storage/localStorage.js";

// export const typesObj = () => {
//     const tasks = getTasks();
//     return {
//         all: tasks.length,
//         active: tasks.filter(t => t.status.includes("active")).length,
//         success: tasks.filter(t => t.status.includes("success")).length,
//     };
// };


// export const onStatusChange = (callback) => {
//     window.addEventListener('tasksUpdated', () => {
//         callback(typesObj());
//     });
// };